<h1>Section 1: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 2: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 3: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 4: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 5: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 6: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 7: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 8: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 9: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 10: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 11: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 12: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 13: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 14: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 15: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 16: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 17: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 18: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 19: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 20: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 21: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 22: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 23: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 24: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 25: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 26: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 27: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 28: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 29: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 30: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 31: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 32: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 33: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 34: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 35: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 36: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 37: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 38: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 39: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 40: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 41: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 42: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 43: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 44: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 45: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 46: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 47: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 48: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 49: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 50: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 51: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 52: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 53: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 54: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 55: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 56: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 57: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 58: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 59: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 60: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 61: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 62: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 63: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 64: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 65: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 66: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 67: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 68: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 69: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 70: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 71: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 72: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 73: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 74: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 75: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 76: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 77: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 78: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 79: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 80: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 81: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 82: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 83: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 84: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 85: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 86: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 87: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 88: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 89: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 90: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 91: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 92: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 93: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 94: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 95: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 96: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 97: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 98: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 99: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 100: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 101: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 102: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 103: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 104: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 105: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 106: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 107: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 108: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 109: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 110: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 111: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 112: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 113: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 114: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 115: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 116: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 117: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 118: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 119: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 120: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 121: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 122: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 123: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 124: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 125: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 126: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 127: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 128: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 129: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 130: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 131: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 132: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 133: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 134: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 135: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 136: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 137: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 138: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 139: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 140: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 141: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 142: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 143: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 144: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 145: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 146: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 147: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 148: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 149: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 150: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 151: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 152: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 153: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 154: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 155: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 156: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 157: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 158: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 159: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 160: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 161: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 162: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 163: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 164: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 165: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 166: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 167: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 168: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 169: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 170: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 171: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 172: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 173: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 174: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 175: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 176: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 177: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 178: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 179: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 180: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 181: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 182: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 183: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 184: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 185: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 186: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 187: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 188: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 189: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 190: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 191: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 192: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 193: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 194: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 195: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 196: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 197: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 198: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 199: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>
<h1>Section 200: Welcome to TSN</h1>
<p>This is a <strong>bold</strong> statement about <em>compiling</em> TypeScript to native code.</p>
<h2>Why Native Compilation?</h2>
<p>The idea is simple: if you have <strong>type information</strong> at compile time, you can emit</p>
<p><code>machine code</code> instead of interpreting through V8.</p>
<h3>The Trade-offs</h3>
<p>Here is a <a href="https://example.com/spec">link to the spec</a> for reference.</p>
<p>Some things you lose:</p>
<p>- Dynamic property access</p>
<p>- The <code>eval</code> function</p>
<p>- Runtime type changes</p>
<pre><code>function add(a: number, b: number): number {
  return a + b
}</code></pre>
<h2>Performance</h2>
<p>With <strong>strict types</strong> and no dynamic features, the compiler knows the exact</p>
<p>memory layout of every object. This means:</p>
<h3>No Boxing</h3>
<p>Numbers are just <code>double</code> values on the stack. No heap allocation.</p>
<h3>No Garbage Collection Pressure</h3>
<p>With arena allocation, we free everything at once at the end.</p>
<h2>Conclusion</h2>
<p>This is a <em>real</em> markdown document with <strong>bold</strong>, <em>italic</em>, <code>code</code>,</p>
<p><a href="https://example.com">links</a>, and code blocks. It exercises the full</p>
<p>parser pipeline.</p>
<h3>Final Thoughts</h3>
<p>The gap between TypeScript and a <strong>compilable language</strong> is smaller than</p>
<p>most people think. We just need to <em>cut the right corners</em>.</p>
<p>---</p>

---
Headers: 1600
Paragraphs: 3600
Code blocks: 200
