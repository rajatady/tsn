/*
 * TSN Native Ops Brief
 *
 * A small native desktop example that polls a hosted JSON endpoint,
 * caches the latest payload to disk, and keeps the result visible in
 * a macOS window. This is the kind of workflow TSN is for:
 * TypeScript syntax driving native UI plus hosted async I/O directly.
 */

import { App } from './ops-brief/app'

<App />
