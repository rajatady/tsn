/*
 * TSN App Store demo — iOS shell
 *
 * This reuses the same routed App Store screens and state as the desktop
 * example, but wraps them in an iPhone-style shell so the screen tree stays
 * shared while the host runtime handles responsive layout underneath.
 */

import { IOSApp } from './app-store/ios-app'

<IOSApp />
